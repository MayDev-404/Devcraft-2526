import ResetPasswordForm from "@/components/ResetPasswordForm";

export const metadata = { title: "Reset password · DevCraft" };

export default function ResetPasswordPage() {
  return <ResetPasswordForm />;
}
